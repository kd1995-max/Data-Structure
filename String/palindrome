	
	int isPlaindrome(string S)
	{
	    
	    int low=0;
    int high=S.length()-1;
    while(low<=high)
    {
        if(S[high] != S[low])
            return 0;
        low++;
        high--;
    }
    return 1;
	}
