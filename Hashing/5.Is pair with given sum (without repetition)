//5.Is pair with given sum (without repetition)

int ispair(int arr[],int n,int sum)
{
	unordered_set<int> s(arr,arr+n);
	for(int i=0;i<n;i++)
	{
		if(s.find(sum-arr[i])
			return true;
		else
			return false;
	}		
}
